import 'package:flutter/material.dart';

class InheritStatelessWidget extends StatelessWidget {
  const InheritStatelessWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
